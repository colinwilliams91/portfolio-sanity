import project from "./project-schema";
import page from "./page-schema";
import photo from "./photo-schema";
import link from "./link-schema";

const schemas = [project, page, photo, link];

export default schemas;
