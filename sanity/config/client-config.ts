import schemas from "../schemas";

const config = {
  projectId: 'coo17utp',
  dataset: 'production',
  useCdn: false,
  apiVersion: '2024-02-05',
  schema: {
    types: schemas
  }
}

export default config;
